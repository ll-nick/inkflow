This slide is itself the example:
a hand-drawn SVG (`slides/diagram.svg`) inheriting the themed `base` layout,
with a `TextBox` zone.
Press the arrow key once to fade in the last box and complete the pipeline.
