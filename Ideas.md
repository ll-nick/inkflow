# Ideas

Roughly ordered by priority

- Plugin system for e.g. typst and mermaid integration. Allow zones to be filled with content generated by runtime-loadable plugins.

- Skip broken slides and just display error/placeholder slide instead of crashing the whole deck?

- `inkflow eject` copies a theme layout into the project and updates `deck.py` to point at the local copy.

- Watch-only mode (just rebuild, no server)

- Configurable keybindings: a `keybindings` dict on `Deck`

- Auto-advance: timed slides for kiosk or lightning-talk use

- Drawing and annotation mode (this one might be too much work for now)
