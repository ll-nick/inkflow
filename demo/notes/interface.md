All navigation state lives on the server. The WebSocket connection syncs slide position across every open window in real
time — no pairing or extra setup needed.

Open the same URL in a second window and press `p` in one of them for a full
presenter view: clock, notes, and a preview of the next slide.
