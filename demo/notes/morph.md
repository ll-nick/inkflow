Morph matches elements by `id` and interpolates geometry and color.
The box moves, resizes, and recolours; the circle scales; the bar grows. A single
transition covers every change at once.

Unmatched elements fade in or out, identical elements are preserved.
