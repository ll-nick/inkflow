inkflow is a code-first SVG presentation tool. You draw slides in any SVG editor,
declare the deck in Python, and inkflow serves it as a live browser presentation.
No install step for the audience — just open a URL.

The cover image here uses `alt_src` to swap automatically between light and dark mode.
