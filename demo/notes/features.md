SVGs are plain text, so every slide is diff-able and version-controlled in git like
any other source file. No proprietary binary format, no lock-in.

Themes and layouts are Python packages: `uv add my-theme` gives a whole team a
consistent look. Export produces a single self-contained HTML file — no server
required, works offline, email it or drop it on a USB stick.
