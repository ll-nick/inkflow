# Markdown & math

Author slides in plain **Markdown** - lists, **bold**, `code`, links.

Real LaTeX renders inline and as display blocks:

$$\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$$

Inline works too: $a^2 + b^2 = c^2$.
